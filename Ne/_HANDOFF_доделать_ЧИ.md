# Хэндофф: доделать ЧИ

Дата: 2026-05-13. Этот файл — план для следующей сессии после компактификации.
Контекст текущей сессии исчерпан. Большая часть ЧИ-контента уже в проде, но есть три
конкретных хвоста: **дедупликация (Холл vs карточка)**, **расширение материала до полных
источников**, **прокликивание в проде**.

---

## Что уже в проде (что НЕ трогать)

### ✅ Полностью готово
- **Skill tree** `slw-main/slw-main/src/data/journey/skills/ne-tree.js` — 36 навыков
- **Анкеты** `ne-skills.js` + `ne-surveys.md` — 540 statements
- **SkillDetail+SkillTraits контент** `data/skills/Ne/{core,sage,pioneer,catalyst,visionary}.js`
- **Skill resistance blocks** `data/skills/Ne/skill-blocks.js` — 36 блоков (suppression/defense/beliefs/family)
- **NeWheel** + **NeSkillTree** — UI компоненты
- **Journey markdown** `aspects/Ne/l0.md..l3.md`

### ✅ В карточке аспекта (`ASPECT_DATA.Ne` в `data/aspects.js`)
Сейчас есть: name, sub, metaphor, essence, superpower, dilemmas (3), archetypes (shadow/gift 3+3),
redFlags (6), fears, defenses, coachTips (7), selfAssessment (5 микрополей × 5 вопросов),
goals (8), resources (8), practices (5), integration (Si opposite), synergy (3), polysemy (3),
somatic (shadow/gift по 4), archetypePath (4), skills (36), myths (44), culturalDifferences (50),
childRaising (50), childhoodQuestions (50), professions (50), neSkillBlocksCore (3), neSkillBlocks (33).

### ✅ В Холле (`HALL_CONTENT.Ne` в `data/hallContent.js`)
quotes (50), figures (20), arts (70), interestingFacts (46), archetypes (4 канонических).

---

## ⚠️ Задача 1 — Дедупликация (CRITICAL, делать первым)

**Проблема:** в `ASPECT_DATA.Ne` сейчас лежат `historicalFigures` (20), `art` (40), `quotes` (20) —
которые **дублируют** `HALL_CONTENT.Ne.{figures, arts, quotes}`. У Fe эти три поля
**отсутствуют** в `ASPECT_DATA.Fe` — они живут только в Холле. На странице аспекта Fe
работают только blocks `hallFigures`, `hallArts`, `hallQuotes` (kind: `hallStub`), которые
показывают «обсуждается в Холле» с кнопкой-переходом.

**Что сделать:**

1. Открыть `slw-main/slw-main/src/data/aspects.js`, найти `ASPECT_DATA.Ne` (~ строка 3213).

2. **Удалить три поля** из `ASPECT_DATA.Ne`:
   - `historicalFigures: [...]` (20 объектов) — строки ~3454-3475
   - `art: [...]` (40 объектов) — строки ~3477-3520
   - `quotes: [...]` (20 объектов) — строки ~3268-3289

3. После этого блоки `historicalFigures`, `art`, `quotes` в `blocks.js` для Ne не пройдут
   проверку `has: d => d.foo?.length > 0` и **просто не покажутся**. Останутся только
   `hallFigures`, `hallArts`, `hallQuotes` — стабы со ссылкой в Холл.

**Сверка с Fe:**
```bash
cd slw-main/slw-main/src/data
python -c "
import re
with open('aspects.js', 'r', encoding='utf-8') as f:
  text = f.read()
fe = re.search(r'ASPECT_DATA\.Fe = \{', text)
fi = re.search(r'ASPECT_DATA\.Fi = \{', text)
fe_text = text[fe.start():fi.start()]
for field in ['historicalFigures', 'art:', 'quotes:', 'professions', 'myths']:
  m = re.search(rf'\\n  {re.escape(field)}', fe_text)
  print(f'{field}: {\"PRESENT\" if m else \"absent\"}')
"
# Должно дать: historicalFigures: absent, art:: absent, quotes:: absent,
#              professions: PRESENT, myths: PRESENT
```

**Должно остаться в `ASPECT_DATA.Ne`** (как у Fe):
- `professions` ✓ (уже есть)
- `myths` ✓ (уже есть)
- Всё остальное (theory полей) ✓

**Билд после удаления:** должен проходить чисто. Никакой код не импортирует эти три поля
напрямую кроме blocks.js, который читает через `has` predicate.

---

## ⚠️ Задача 2 — Полное расширение материала

### 2A. Холл — расширить до полного покрытия источников

Источники в папке `Ne/`:

| Что | Сейчас в HALL_CONTENT.Ne | Источник | Цель |
|---|---|---|---|
| quotes | 50 | `цитаты ЧИ.md` (80) | расширить до **80** |
| arts | 70 | `Искусство ЧИ.md` (95) | расширить до **95** |
| interestingFacts | 46 | `интересные факты по ЧИ.md` (90) | расширить до **90** |
| figures | 20 | `исторические личности ЧИ.md` (20) | уже полные ✓ |

**Структура Холла** (`HALL_CONTENT.Ne` в `data/hallContent.js`, начало ~ строка 14):
```js
Ne: {
  quotes: [
    { text: '...', author: '...', note: 'Комментарий связи с архетипом ЧИ.' },
    // ...
  ],
  figures: [
    { name: 'Дар. Имя (даты) · Архетип', note: 'Биография + связь с архетипом.' },
    // ...
  ],
  arts: [
    { type: 'book'|'film'|'painting'|'music', title: '...', note: 'Связь с ЧИ.' },
    // ...
  ],
  interestingFacts: [
    { name: 'Короткий заголовок', desc: 'Полное описание факта.' },
    // ...
  ],
  archetypes: [...] // уже 4 канонических, не трогать
}
```

**arts**: типы должны быть `book`, `film`, `painting`, `music`. Для сериалов использовал
`type: 'film'` с префиксом «Сериал «...»» в title — пометить так же. Для скульптур, архитектуры,
театра — у Fe нет, можно использовать `painting` или новые типы, но проверить рендер в
HallView перед тем как добавлять новые типы.

**Дополнения по разделам (что добавить из источников):**

*quotes 50 → 80:* источник содержит 80 цитат с пояснениями. Сейчас взяты первые 20 для
карточки + 50 для Холла (фактически расширенный набор). Нужно дополнить **остальные 30** из
`цитаты ЧИ.md` (цитаты 26-30, 38-50, 51-80 в нумерации источника).

*arts 70 → 95:* добавить **25 произведений**, особенно:
- Из источника 15 сериалов: в Холле сейчас 7 сериалов, добавить остальные 8
  (Утопия/Utopia, Рик и Морти, Видоизменённый углерод, 1899, Хорошее место,
  Тёмные начала, Настоящий детектив, Очень странные дела)
- Из источника 6 скульптур (Микеланджело-Давид, Роден-Мыслитель, Бранкузи-Колонна,
  Джакометти-Идущий человек, Аниш Капур-Cloud Gate, Ай Вэйвэй-Подсолнечные семечки)
- Из источника 6 архитектура (можно добавить Капеллу Роншан Ле Корбюзье, Биосферу
  Бакминстера Фуллера, Гейдара Алиева Захи Хадид, Hundertwasserhaus Хундертвассера)
- Из источника 5 театр (Брехт-Галилей, Беккет-Годо, Ионеско-Носорог, Стоппард-Розенкранц,
  Брук-Махабхарата)
- Книги/фильмы — добавить недостающие из source

*interestingFacts 46 → 90:* источник в 10 категориях. Добавить из категорий, которых
пока нет/мало в Холле:
- Сны и творческие открытия (10) — частично есть, добавить пропущенное
- Метафоры/язык/видение (8) — добавить про Сепира-Уорфа, китайский 危机, японский ma
- Латеральное мышление и парадоксы (7) — Эдвард де Боно метод PO, синектика Гордона
- Тёмная сторона видения (7) — Даннинг-Крюгер, конспирологическое мышление, эффект сверхвидения
- Культурные практики работы с ЧИ (6) — wabi-sabi, gemba, дзенские коаны, фронесис

### 2B. Карточка аспекта — расширить professions

`ASPECT_DATA.Ne.professions` сейчас содержит 50 из 107. Источник: `профессии ЧИ.md` (1361
строка, 107 профессий в 11 категориях). Структура каждой записи:
```js
{ name: 'Название', desc: 'Что именно делает ЧИ в этой профессии (2-3 предложения).' }
```

**Что добавить (57 профессий):** все из категорий, которые сейчас слабо покрыты.
Текущий охват и пробелы:

- I. Стратегия (15) — в проде 10, добавить 5: Brand strategist, Портфельный стратег, Patent attorney, Корпоративный антрополог (есть?), Этнограф потребительских привычек
- II. Творчество (15) — в проде ~10, добавить 5: Драматург, Хореограф, Литературный редактор, Литературный агент, Композитор
- III. Психология (15) — в проде ~10, добавить 5: Семейный психотерапевт, Гештальт-терапевт, Семейный системный терапевт, Drama therapist, Art therapist, Логотерапевт, Coach для подростков, Медиатор
- IV. Образование и наука (12) — в проде 5, добавить 7: Школьный учитель литературы, Преподаватель философии, Социолог, Лингвист, Междисциплинарный учёный, Архивист
- V. Технологии (12) — в проде ~6, добавить 6: Software architect, AI-стратег, Prompt engineer, Геймдизайнер игр-симуляторов, Permaculture designer, Архитектор зданий, Service designer для государства
- VI. Журналистика (10) — в проде ~5, добавить 5: Репортёр-международник, Travel-writer, Литкритик, Кинокритик, Документальный фотограф, Editor-in-chief, Talk-show host
- VII. Социальные практики (5) — в проде ~3, добавить 2: NGO-стратег, Активист новых движений, Cultural strategist, Community designer
- VIII. Дипломатия (5) — в проде ~1, добавить 4: Аналитик внешней политики, Cross-cultural consultant, Crisis manager, PR-стратег
- IX. Маркетинг (5) — в проде ~3, добавить 2: Копирайтер, Маркетинговый стратег
- X. Особые (6) — в проде ~4, добавить 2: Speech writer, Innovation broker
- XI. Наука как видение (7) — в проде ~5, добавить 2: Математик-исследователь, Когнитивный нейроучёный, Астроном-исследователь

Точный список профессий с их описаниями — в `Ne/профессии ЧИ.md` (читать секциями по
эмодзи-заголовкам).

### 2C. Карточка — расширить myths (опционально)

В `ASPECT_DATA.Ne.myths` сейчас 44 мифа из 50 в источнике `Мифы и Боги ЧИ.md`. Пропущены
6 (нужно посмотреть какие — обычно из тех, что попадают на границу ЧИ/БИ). Можно дополнить
до 50, но это nice-to-have.

---

## ⚠️ Задача 3 — Прокликать всё в проде

URL: `https://sergeshaneri.github.io/slw` (логин юзера).

**Пройти по пути ЧИ:**

1. **Карта планет** → выбрать «Essence Prime» (ЧИ)
2. **Дашборд → Аспекты → Чёрная Интуиция** — проверить рендер карточки:
   - L0 блоки: essence, archetypes, neSkillBlocksCore (открыто всегда)
   - L1 блоки: при `journey.aspects.Ne.currentLevel < 1` должны быть в тизер-режиме
   - L2, L3 — соответственно
3. **Открыть каждый блок** — проверить, что текст не вылезает за рамки, list-items
   корректно нумерованы, не сломан markdown
4. **Холл ЧИ** — открыть, проверить quotes/figures/arts/interestingFacts
5. **NeSkillTree** — клик на любой навык → анкета → пройти 5 утверждений → SurveyInsight →
   «Сохранить и узнать, как развить» → SkillDetail → «Какие психологические черты» → SkillTraits
6. **NeWheel** — на странице Аспекты для ЧИ должно показывать колесо с 4 архетипами,
   средними оценками, звёздами

**Что специально проверить:**
- Карточка ЧИ показывает разделы `hallFigures`/`hallArts`/`hallQuotes` (стаб со ссылкой в Холл),
  а НЕ собственные `historicalFigures`/`art`/`quotes` (после Задачи 1 они должны исчезнуть)
- Блок `neSkillBlocksCore` на L0 показывает 3 ядерных навыка (внимание к сути, метапознание,
  mindfulness) с 4 секциями каждый (вытеснение, защиты, убеждения, родовые программы)
- Блок `neSkillBlocks` на L3 показывает все 33 архетипных навыка (по 7+7+10+9). При `cl<3`
  должен быть в тизер-режиме (1 первый навык виден, остальные размыты + кнопка достичь L3)
- Bundle размер: 5.9 MB сейчас. Если станет >7 MB после расширения — стоит задуматься о
  code-splitting (но это отдельная задача).

---

## Ключевые файлы (быстрая шпаргалка)

```
slw-main/slw-main/src/
├── data/
│   ├── aspects.js                  # ASPECT_DATA.Ne (Задача 1, 2B, 2C)
│   ├── hallContent.js              # HALL_CONTENT.Ne (Задача 2A)
│   ├── journey/skills/
│   │   ├── ne-tree.js              # 36 навыков ЧИ (не трогать)
│   │   ├── ne-skills.js            # парсер анкет (не трогать)
│   │   └── ne-surveys.md           # 540 statements (не трогать)
│   └── skills/Ne/
│       ├── core.js, sage.js, pioneer.js, catalyst.js, visionary.js
│       ├── skill-blocks.js         # 36 блоков сопротивления (не трогать)
│       └── index.js                # объединяет всё (не трогать)
├── components/AspectsView/
│   ├── NeWheel.jsx                 # колесо ЧИ
│   ├── blocks.js                   # 30 блоков теории аспекта (зарегистрирован Ne)
│   └── AspectsView.jsx             # роутер карточки
└── components/JourneyView/
    ├── NeSkillTree.jsx
    └── SkillDetail.jsx + SkillTraits.jsx (универсальные)
```

Источники в `slw-slw-instruct/Ne/`:
- `цитаты ЧИ.md` — 80 цитат
- `интересные факты по ЧИ.md` — 90 фактов
- `Искусство ЧИ.md` — 95 произведений
- `исторические личности ЧИ.md` — 20 фигур (полный охват)
- `профессии ЧИ.md` — 107 профессий
- `Мифы и Боги ЧИ.md` — 50 мифов
- `Культурные отличия ЧИ.md` — 50 пунктов (полный охват)
- `как привить ребенку ЧИ.md` — 50 пунктов (полный охват)
- `вопросы про детство ЧИ.md` — 50 вопросов (полный охват)
- `блоки навыков ЧИ.md` — 36 блоков (полный охват)

## Порядок работы для следующей сессии

1. **Задача 1** (дедупликация) — 20 минут. Удалить 3 поля из `ASPECT_DATA.Ne`. Билд + коммит.
2. **Прокликать после Задачи 1** — убедиться, что Холл-стабы появились, а собственные разделы
   исчезли. 10 минут.
3. **Задача 2A** (Холл расширение) — 1-2 часа. Чтение источников + расширение трёх массивов.
   Билд + коммит.
4. **Задача 2B** (professions) — 1 час. Добавление 57 профессий. Билд + коммит.
5. **Задача 3** (полный прокликивание) — 30 минут. Логин в прод, проход всего флоу ЧИ.
6. **Деплой**: `cd slw-main/slw-main && npm run deploy`.

## Команды быстрого старта

```bash
# Git состояние
cd /c/Serge/slw-slw-instruct/slw-slw-instruct
git log --oneline -5

# Прогон билда
cd slw-main/slw-main
npm run build

# Деплой
npm run deploy

# Сверка с Fe (что есть в ASPECT_DATA)
cd src/data
python -c "
import re
with open('aspects.js', 'r', encoding='utf-8') as f:
  text = f.read()
fe = re.search(r'ASPECT_DATA\.Fe = \{', text)
fi = re.search(r'ASPECT_DATA\.Fi = \{', text)
fe_text = text[fe.start():fi.start()]
for field in ['historicalFigures', 'art:', 'quotes:', 'professions', 'myths']:
  m = re.search(rf'\\n  {re.escape(field)}', fe_text)
  print(f'{field}: {\"PRESENT\" if m else \"absent\"}')
"

# Структурная валидация Ne skill-blocks
node -e "
import('./src/data/skills/Ne/skill-blocks.js').then(m =>
  import('./src/data/journey/skills/ne-tree.js').then(t => {
    const seen = new Set(m.NE_SKILL_BLOCKS.map(b => b.skillId))
    const tree = new Set(t.ALL_SKILL_IDS)
    console.log('blocks:', seen.size, 'tree:', tree.size,
      'missing:', [...tree].filter(x => !seen.has(x)))
  }))
" --input-type=module
```

## Состояние gh-pages

Последний деплой содержит:
- ASPECT_DATA.Ne со ВСЕМИ полями включая дубли (historicalFigures/art/quotes)
- HALL_CONTENT.Ne расширен до уровня Fe
- skill-blocks.js — все 36 блоков

После Задачи 1 деплоем юзер увидит чистую структуру (как у Fe).
После Задачи 2 деплоем юзер увидит полное покрытие источников.
